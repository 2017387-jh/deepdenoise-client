<Window x:Class="DeepDenoiseClient.Views.Dialog.ProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Profile" Height="260" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Profile Name"/>
        <TextBox Grid.Row="1" Margin="0,4,0,12" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="2" Text="API Base URL"/>
        <TextBox Grid.Row="3" Margin="0,4,0,12" Text="{Binding ApiBase, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="0,0,8,0" IsDefault="True" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>